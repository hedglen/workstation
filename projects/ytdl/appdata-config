# Linked by install.ps1 → %APPDATA%\yt-dlp\config (global yt-dlp CLI defaults).
# Wrapper presets live in config.toml next to ytdl.py.

# Audio defaults
--extract-audio
--audio-format flac
--audio-quality 0
--embed-thumbnail
--embed-metadata
--output "D:\Media\%(uploader)s\%(title)s.%(ext)s"
--sponsorblock-remove all
